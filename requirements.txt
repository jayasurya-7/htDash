# htDash — HOMER Therapy Dashboard
# Training Simulator & Main Application Dependencies
#
# Usage:
#   pip install -r requirements.txt
#
# Or with UV (recommended):
#   uv sync
#
# Python Requirements: 3.13+
#

# Core Framework
Flask>=3.1.3              # Web framework for main app
Werkzeug>=3.0.0         # WSGI utilities

# Authentication & Security
bcrypt>=5.0.0            # Password hashing (secure credential storage)

# AWS Integration
boto3>=1.43.46           # AWS SDK (S3 file storage)
botocore>=1.46.46        # AWS SDK core

# Data Processing
pandas>=3.0.3            # Data manipulation & analysis

# Timezone Support
pytz>=2026.2             # Timezone utilities (IST for Ranipet/Manipal/Ludhiana)

# PDF & Report Generation
reportlab>=5.0.0         # PDF generation for prescriptions/assessments
html2image>=1.1.0        # Image rendering for PDFs

# QR Code Generation (for exercise pamphlets)
qrcode>=8.2              # QR code creation
Pillow>=10.0.0           # Image processing (dependency of qrcode)

# Optional: PDF editing (commented out by default)
# PyPDF>=3.0.0           # PDF manipulation if needed

# Optional: Database (if moving from JSON to database in future)
# SQLAlchemy>=2.0.0      # ORM layer
# psycopg2-binary>=2.9.0 # PostgreSQL adapter

# Optional: Testing (for running test suite)
# pytest>=7.0.0          # Test runner
# pytest-cov>=4.0.0      # Coverage reporting

# Optional: Development
# black>=23.0.0          # Code formatter
# flake8>=6.0.0          # Linter
# mypy>=1.0.0            # Type checker
